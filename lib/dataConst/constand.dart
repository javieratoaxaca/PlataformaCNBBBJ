import 'dart:ui';

  ///El archivo `constand` contiene los valores de colores utilizados en la UI del proyecto,
  ///los colores son proporcionados por la dependencia.

const greenColorLight = Color(0xFF1B5C4B); // Color para algunos componentes de la UI

const greenColorDark = Color(0xFF002F2A);

const wineLight = Color(0xFF9B2247);

const wineDark = Color(0xFF611232);

const darkBackground = Color(0xFFBF8F54); // Color de fondo oscuro de la UI

const lightBackground = Color(0xFFD9BC9A); // Color de fondo claro de la UI

const light = Color(0xFFF2E8DC); // Color claro de la UI

const dark = Color(0xFF595756); // Color oscuro de la UI

const pantone1255 = Color(0xFFA57F2C);

const pantone872c = Color(0xFFE6D194);